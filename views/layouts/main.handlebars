<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{title}}</title>
    <style>
      :root{ --bg:#0f172a; --card:#111827; --text:#e5e7eb; --muted:#94a3b8;
      --accent:#3b82f6; } *{ box-sizing:border-box; } body{ margin:0;
      background:var(--bg); color:var(--text); font-family:system-ui,Segoe
      UI,Roboto,Ubuntu,"Helvetica Neue",Arial,"Noto Sans",sans-serif; } header,
      main{ max-width:1000px; margin:0 auto; padding:24px; } header h1{ margin:0
      0 6px 0; font-weight:700; letter-spacing:.3px; } header p{
      color:var(--muted); margin:0; } .card{ background:var(--card); border:1px
      solid #1f2937; border-radius:14px; padding:18px; box-shadow:0 10px 20px
      rgba(0,0,0,.25); } footer{ max-width:1000px; margin:28px auto; padding:0
      24px; color:var(--muted); font-size:.9rem; } a{ color:var(--accent);
      text-decoration:none; } .btn{ display:inline-block; width:100%;
      padding:12px 16px; border-radius:10px; border:1px solid #0b1220;
      background:#0b1220; color:#fff; font-weight:600; text-align:center; }
      .grid{ display:grid; gap:12px; } .input{ width:100%; padding:12px 14px;
      border-radius:10px; border:1px solid #374151; background:#0b1220;
      color:#e5e7eb; } .muted{ color:var(--muted); } .notice{
      background:#111827; border:1px dashed #334155; color:#cbd5e1;
      padding:14px; border-radius:10px; } .list{ background:#0b1220;
      border-radius:12px; border:1px solid #1f2937; } .row{ padding:12px 14px;
      display:grid; grid-template-columns: 2fr 1fr 1fr; gap:10px;
      border-bottom:1px solid #1f2937; } .row:last-child{ border-bottom:0; }
    </style>
  </head>
  <body>
    <header>
      <h1>{{title}}</h1>
      <p>Search NYC restaurants by page, page size, and borough.</p>
    </header>
    <main>
      {{{body}}}
    </main>
    <footer>&copy; {{year}} ITE5315</footer>
  </body>
</html>